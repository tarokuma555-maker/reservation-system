/**
 * この置き場所を「デモ」として動かすかどうか。
 *
 * 同じコードを、本番用とデモ用の2つの置き場所で動かしている。
 * 見ているデータベースが違うので中身は混ざらないが、
 * 画面の見た目と、うっかり実際のお客様に送ってしまう危険の扱いだけが違う。
 *
 * デモ側の Vercel で DEMO_MODE=1 を設定する。付けなければ本番として動く。
 */
export function isDemoMode(): boolean {
  return process.env.DEMO_MODE === "1";
}
