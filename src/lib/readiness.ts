/**
 * 「まだお客様に見せてはいけない機能」を、コード側で塞いでおくための目印。
 *
 * 画面に注意書きを書くだけでは、いつか誰かが押してしまう。
 * 実装が終わるまではボタン自体を押せなくしておき、
 * 終わったらここを true に変える。
 */

/**
 * LIFFで「いま誰が開いているか」を見分けられるか。
 *
 * LINEの発行する証明をサーバーで確かめ、確認できた方のデータだけを出すようにした。
 * 分からないときは中身を一切表示しない（かつては最初のお客様を出していた）。
 * あわせて、予約の作成・変更・取り消しも、ご本人のものだけを通すようにしている。
 *
 * 実際にお客様に使っていただくには、LIFF IDの設定も必要。
 * それが入っているかは実行時に確かめる（isLiffLive）。
 */
export const LIFF_IDENTITY_READY = true;
